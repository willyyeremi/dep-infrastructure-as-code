# [DEP] Infrastructure as Code
This project contains files to define the infrastructure that has been used as code. The list below will show details about the tools being used, their functions, and the versions.

- VirtualBox
  - version:
  - function: Used as a hypervisor of VMs. VM needed to simulate real real-world case where one microservice runs on one machine.
- K3s
  - version:
  - function: Used as a container orchestrator.
- Docker
  - version:
  - function: Used as a container executor.

